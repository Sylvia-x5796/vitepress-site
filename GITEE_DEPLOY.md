# Gitee Pages 部署指南

## 🚀 部署到 Gitee Pages

### 仓库地址
https://gitee.com/sylviaxiao/vite-press_-sylviax

### 快速部署步骤

#### 1. 安装部署工具

```bash
npm install
```

#### 2. 构建并部署

```bash
# 一键构建并部署
npm run build:deploy
```

#### 3. 在 Gitee 上启用 Pages 服务

1. 访问仓库页面：https://gitee.com/sylviaxiao/vite-press_-sylviax
2. 点击 "服务" → "Gitee Pages"
3. 选择 `gh-pages` 分支
4. 点击 "启动" 或 "更新" 按钮
5. 等待部署完成（通常需要几分钟）

### 访问地址

部署成功后，网站将通过以下地址访问：

**https://sylviaxiao.gitee.io/vite-press_-sylviax**

## 📋 详细说明

### 部署原理

1. `npm run docs:build` - 构建静态文件到 `docs/.vitepress/dist`
2. `npm run deploy` - 使用 gh-pages 工具将 dist 目录推送到 Gitee 仓库的 `gh-pages` 分支
3. Gitee Pages 服务从 `gh-pages` 分支读取文件并发布

### 首次部署

如果是第一次部署，需要先配置 Git 仓库：

```bash
# 初始化 Git（如果还没有）
git init

# 添加远程仓库
git remote add origin https://gitee.com/sylviaxiao/vite-press_-sylviax.git

# 提交代码到主分支
git add .
git commit -m "Initial commit"
git push -u origin master
```

### 更新网站内容

每次修改内容后：

```bash
# 方式1：只部署（推荐）
npm run build:deploy

# 方式2：分步执行
npm run docs:build
npm run deploy
```

### 配置 base 路径

如果你的 Gitee Pages 地址包含子路径（如 `/vite-press_-sylviax`），需要在 `docs/.vitepress/config.mts` 中配置：

```typescript
export default defineConfig({
  base: '/vite-press_-sylviax/',  // 注意前后都有斜杠
  // ... 其他配置
})
```

**当前配置：** `base: '/'` （适用于自定义域名或根路径部署）

如果访问时样式丢失，需要修改为：`base: '/vite-press_-sylviax/'`

## 🔧 常见问题

### 1. 部署失败：权限问题

确保你已经登录 Git 并有仓库的推送权限：

```bash
# 配置 Git 用户信息
git config --global user.name "你的用户名"
git config --global user.email "你的邮箱"

# 使用 HTTPS 方式推送时会要求输入用户名和密码
```

### 2. Gitee Pages 不更新

Gitee Pages 免费版需要手动点击"更新"按钮：

1. 进入仓库页面
2. 服务 → Gitee Pages
3. 点击"更新"按钮
4. 等待重新部署

### 3. 样式或资源加载失败

检查 `base` 配置是否正确：

- 如果访问地址是 `https://sylviaxiao.gitee.io/vite-press_-sylviax/`
- 则 `base` 应该设置为 `'/vite-press_-sylviax/'`

### 4. 404 错误

确保：
- `gh-pages` 分支已经创建并包含构建文件
- Gitee Pages 服务已启用并选择了正确的分支
- 访问的 URL 路径正确

## 📝 部署检查清单

- [ ] 安装了 gh-pages 依赖
- [ ] 配置了正确的仓库地址
- [ ] 执行 `npm run build:deploy` 成功
- [ ] 在 Gitee 上启用了 Pages 服务
- [ ] 选择了 `gh-pages` 分支
- [ ] 点击了"更新"按钮
- [ ] 等待部署完成（查看状态）
- [ ] 访问网站确认部署成功

## 🔄 自动化部署（可选）

### 使用 Gitee Pages Pro

升级到 Gitee Pages Pro 可以实现：
- 自动更新（无需手动点击）
- 更快的构建速度
- 更多的流量配额

### 使用 GitHub Actions

如果你同时使用 GitHub，可以配置 Actions 自动同步到 Gitee：

1. 在 GitHub 上配置 Actions
2. 自动构建并推送到 Gitee
3. 触发 Gitee Pages 更新

## 📞 获取帮助

如果遇到问题：

1. 查看 Gitee Pages 官方文档：https://gitee.com/help/articles/4136
2. 检查浏览器控制台错误信息
3. 确认 `gh-pages` 分支内容是否正确
4. 发送邮件到 410676208@qq.com

## 🎉 部署成功后

访问你的网站：**https://sylviaxiao.gitee.io/vite-press_-sylviax**

记得在社交媒体上分享你的新网站！
