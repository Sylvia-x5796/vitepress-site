{
  "name": "vitepress-site",
  "version": "1.0.0",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "deploy": "gh-pages -d docs/.vitepress/dist -r https://gitee.com/sylviaxiao/xiao-xis-personal-homepage.git",
    "build:deploy": "npm run docs:build && npm run deploy"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vitepress": "^0.22.4"
  }
}
